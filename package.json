{
  "name": "server",
  "version": "0.0.1",
  "description": "Server side app in NodeJS that handles communication between messenger users and archive their messages in external DB",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "(https://github.com/Witek93/server.git)"
  },
  "author": "Karol Witaszczyk",
  "license": "ISC",
  "dependencies": {
    "mqtt": "^1.3.5",
    "pg": "^4.4.0",
    "pg-transaction": "^1.0.4"
  }
}
